---
slurmd:
  volumeMounts:
    - name: dcgm-job-mapping
      mountPath: '{{ include "vendor.dcgm.jobMappingDir" $ }}'
podSpec:
  volumes:
    - name: dcgm-job-mapping
      hostPath:
        type: DirectoryOrCreate
        path: '{{ include "vendor.dcgm.jobMappingDir" $ }}'
